<% provide(:title, '温泉ガチ勢一覧') %>
<h1>温泉ガチ勢一覧</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>